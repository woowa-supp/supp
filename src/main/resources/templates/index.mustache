{{>layout/header}}

<h1>supp</h1>
<h3>나는 어떤 개발자일까요?</h3>
<div class="col-md-12">
    <div class="row">
        <div class="col-md-6">
            {{#loginName}}
                Logged in as: <span id="user">{{loginName}}</span>
                <a href="/logout" class="btn btn-info active" role="button">Logout</a>
                {{#isTypeSurveyDone}}
                    <a href="/survey-type" role="button" class="btn btn-primary">수정</a>
                {{/isTypeSurveyDone}}
                {{^isTypeSurveyDone}}
                    <a href="/survey-type" role="button" class="btn btn-primary">시작</a>
                {{/isTypeSurveyDone}}
            {{/loginName}}
            {{^loginName}}
                <a href="/oauth2/authorization/github" class="btn btn-success active" role="button">Github Login</a>
            {{/loginName}}
        </div>
    </div>
</div>

{{>layout/footer}}