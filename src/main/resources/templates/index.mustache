{{>layout/header}}

<h1>supp</h1>
<h3>나는 어떤 개발자일까요?</h3>
<div class="col-md-12">
    <div class="row">
        <div class="col-md-6">
            {{#crewName}}
                Logged in as: <span id="user">{{crewName}}</span>
                <a href="/logout" class="btn btn-info active" role="button">Logout</a>
                <a href="/survey" role="button" class="btn btn-primary">시작</a>
            {{/crewName}}
            {{^crewName}}
                <a href="/oauth2/authorization/github" class="btn btn-success active" role="button">Github Login</a>
            {{/crewName}}
        </div>
    </div>
</div>

{{>layout/footer}}
